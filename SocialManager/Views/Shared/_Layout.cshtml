<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("my Social Manager", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li class="dropdown"><a class="dropdown-toggle" data-toggle="dropdown" href="#">Facebook<b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("Profile", "Index", "Facebook")</li>
                            <li>@Html.ActionLink("Search", "Index", "FacebookSearch")</li>
                            <li>@Html.ActionLink("Post", "Index", "FacebookPost")</li>
                            <li>@Html.ActionLink("Photo", "Index", "FacebookPhoto")</li>
                            <li>@Html.ActionLink("Real Time Update", "Manage", "FacebookSubscription")
                            </li>
                            <li class="divider"></li>
                            <li>@Html.ActionLink("Revoke", "FB_RevokeAccessToken", "Facebook")</li>
                            <li>@Html.ActionLink("Test API Limit", "FB_TestApiLimit", "Facebook")</li>
                        </ul>
                    </li>
                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()

        <hr />
        <footer>
            <p>
                <span class="col-xs-6">
                    &copy; @DateTime.Now.Year - Sample Facebook ASP.NET MVC Application
                </span>
                @if (ViewBag.GlobalStatusMessage != null)
                {
                    <span class="text-danger text-center col-xs-4">@ViewBag.GlobalStatusMessage</span>
                }
            </p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/SignalRScripts")
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="~/signalr/hubs"></script>
    @Scripts.Render("~/bundles/CommonFunctions")
    @RenderSection("scripts", required: false)
</body>
</html>
