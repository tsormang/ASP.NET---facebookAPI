@{
    ViewBag.Title = "Error";
}

<h2 class="text-danger">Error</h2>
<div class="row">
    <div class="col-xz-2">
        @if (ViewBag.ErrorMessage != null)
        {
            <h3 class="text-danger">@ViewBag.ErroMessage</h3>
        }
    </div>
</div>

